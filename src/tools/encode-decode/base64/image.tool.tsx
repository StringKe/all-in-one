'use client';

export function EncodeDecodeBase64ImageTool() {
    return <div>image base64 tool</div>;
}

EncodeDecodeBase64ImageTool.metadata = {
    tKey: 'tools:EncodeDecodeBase64Image',
    icon: undefined,
};
